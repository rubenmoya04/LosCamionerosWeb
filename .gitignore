# ═════════════════════════════════════════════════════
# .gitignore – LosCamionerosWeb (Next.js + Vercel 2025)
# ═════════════════════════════════════════════════════

# Dependencias
node_modules/
pnpm-lock.yaml
package-lock.json
yarn.lock

# Builds y cachés de Next.js
.next/
out/
dist/
build/
.vercel_build_output/

# Vercel
.vercel

# Variables de entorno → NUNCA subir credenciales
.env
.env.local
.env.development.local
.env.production.local
.env*.local

# Pero sí permitimos el ejemplo público
!.env.example

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log
logs/

# Archivos temporales y del sistema
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Thumbs.db:encryptable
ehthumbs_vista.db

# IDEs y editores
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json      # opcional: si quieres compartir config
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Carpetas temporales
.tmp/
.temp/
temp/
tmp/

# Cache de herramientas
.eslintcache
.prettiercache
.stylelintcache

# Base de datos JSON → ¡¡NO SUBIR DATOS REALES!!
# Solo subimos la estructura vacía o de ejemplo
public/dishes-data.json
public/audit-log.json

# Pero permitimos archivos de ejemplo o plantilla
!public/dishes-data.example.json
!public/audit-log.example.json

# Imágenes subidas por el admin (nunca subirlas al repo)
public/uploads/
public/uploads/**
# Mantenemos la carpeta vacía para que Vercel la cree
!public/uploads/.gitkeep

# Imágenes del bar/restaurante → estas SÍ van al repo (son estáticas)
# (están en public/FotosBar/ → no las ignoramos)

# Otros
.env.backup
.env.save
.env.old
coverage/
.storybook/
*.pid
*.seed
*.pid.lock